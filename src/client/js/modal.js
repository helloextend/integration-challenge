
var sendBtn = document.getElementById("btnSend").addEventListener('click', () => {
    document.body.classList.add("show-modal")}
    );

var background = document.getElementById("background").addEventListener('click', () => {
    document.body.classList.remove("show-modal")}
    );

var btnContinue = document.getElementById("continue").addEventListener('click', () => {
    document.body.classList.remove("show-modal")}
    );
    
var closeModal = document.getElementById("btnBusSize").addEventListener('click', () =>{
    document.body.classList.remove("show-modal")}
    );
var getBtn = document.getElementById("btnGet").addEventListener('click', () => {
    document.body.classList.add("show-modal")}
);



 

